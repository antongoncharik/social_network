import React from 'react';
import s from './Footer.module.css';

const Footer = (props) => {
    return (
        <footer className={s.footerBlock}>
            Social Network Best Friends by Anton
        </footer>
    )
}

export default Footer;
