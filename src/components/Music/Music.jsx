import React from 'react';
import s from './Music.module.css';

const Music = (props) => {
    return (
        <div className={s.musicBlock}>
            Music
        </div>
    )
}

export default Music;
